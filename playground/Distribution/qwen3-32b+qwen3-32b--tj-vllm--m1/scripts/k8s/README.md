# Kubernetes Deployment

This directory is reserved for a Kubernetes implementation of the case.
The current M1 deployment is daemon-native and does not use Kubernetes.

Any future manifests must preserve the case topology, service names, ports,
model path, TP4 graph configuration, and health checks documented in the
parent README.
