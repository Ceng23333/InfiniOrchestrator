#!/usr/bin/env bash
set -euo pipefail

HARNESS_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="${HARNESS_ROOT}:${PYTHONPATH:-}"
exec python3 -m adapters.llm_d_benchmark.runner "$@"
