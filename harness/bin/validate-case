#!/usr/bin/env bash
# Unified case diagnostic validator (M0 alpha).
set -euo pipefail

_HARNESS_BIN_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck disable=SC1091
source "${_HARNESS_BIN_DIR}/../lib/paths.sh"

exec python3 -m case_diagnostic.cli "$@"
