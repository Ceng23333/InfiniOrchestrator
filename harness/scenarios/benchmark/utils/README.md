# Shared helpers for scenarios/benchmark cases.
#
# Import path (from case client scripts, with HARNESS_ROOT on PYTHONPATH or via
# relative path under scenarios/benchmark):
#   from scenarios.benchmark.utils.openai_client import chat_completions_url, ...
#
# Or add this directory's parent (scenarios/benchmark) to sys.path and:
#   from utils.openai_client import ...
