# Unexpected-behavior defaults (override via env).
MODEL="${MODEL:-9g_8b_thinking}"
SCENARIO_TIMEOUT_SEC="${SCENARIO_TIMEOUT_SEC:-120}"
POST_FAULT_WAIT_SEC="${POST_FAULT_WAIT_SEC:-5}"
SCENARIOS="${SCENARIOS:-happy_single,cancel_mid_decode,client_disconnect_stream,curl_timeout_burst,concurrent_mixed}"
