## Slave host — infinilm-metax-deployment-opt-20260714
## Copy: cp .env.slave.example .env
## Replace <MASTER_IP> and <SLAVE_IP> with LAN IPs (only placeholders in this file).

IMAGE_TAG=infinilm-svc:metax-hpcc-ai370-PLACEHOLDER

SLAVE_REGISTRY_URL=http://<MASTER_IP>:18000
SLAVE_ROUTER_URL=http://<MASTER_IP>:8800
SLAVE_ADVERTISE_HOST=<SLAVE_IP>

XIYAN_QWENCODER_DIR=/data-aisoft/zenghua/models/XGenerationLab/XiYanSQL-QwenCoder-32B-2504
