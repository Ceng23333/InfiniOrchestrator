# deploy/cases (moved)

Cluster launch recipes now live under [`playground/Distribution/`](../../playground/Distribution/).

`deploy/` remains a **packaging overlay** (offline pack, export-bundle, shared compose fragments in `deploy/docker-compose/`) that references a playground Distribution case id.
