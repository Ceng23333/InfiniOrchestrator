apiVersion: 1

providers:
  - name: ops-panel
    orgId: 1
    folder: Ops Panel
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
